# Programa de teste em Caju

-> numero fatorial(numero n)
inicio
    se (n <= 1)
        retorne 1.
    senao
        retorne n * fatorial(n - 1).
fim

-> vazio principal()
inicio
    numero x := 5.
    exibir(fatorial(x)).
fim